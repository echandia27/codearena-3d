/**
 * Configuración de la aplicación.
 * Si algún día se despliega el frontend, aquí se cambia la URL del backend
 * (o se agrega un environment.prod.ts con fileReplacements en angular.json).
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3001',
};